# Speech-to-text_chrome
A simple GoogleChrome Extension to convert speech to text and copy it to the clipboard.

## Setup

- Host `index.html` to GitHub Pages
- Add `/extensions` to your `chrome://extensions/` in Developer Mode
